-- Preserve validation result IDs generated by /api/events as stable text IDs.
-- Apply this to databases that were initialized before 001_metadata_schema.sql
-- changed event_validation_results.id from UUID to TEXT.

ALTER TABLE event_validation_results
  ALTER COLUMN id DROP DEFAULT,
  ALTER COLUMN id TYPE TEXT USING id::text;
