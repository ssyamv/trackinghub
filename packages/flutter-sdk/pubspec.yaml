name: trackinghub_flutter
description: TrackingHub Flutter SDK for shared event envelope emission.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
